
  # Dark Theme Homepage Design

  This is a code bundle for Dark Theme Homepage Design. The original project is available at https://www.figma.com/design/muGyQ0ilNzL5WVsLeGwvWT/Dark-Theme-Homepage-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  